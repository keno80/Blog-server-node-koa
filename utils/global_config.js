const config = {
  port: 9090,

  database: {
    database: 'blog',
    user: 'root',
    password: 'password',
    port: '3306',
    host: 'localhost',
    multipleStatements: true
  }
}

module.exports = config
